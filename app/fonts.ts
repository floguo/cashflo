import { Instrument_Sans } from 'next/font/google'

export const instrumentSans = Instrument_Sans({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-instrument-sans',
  weight: ['400'],  // Instrument Sans has 400 and 500 weights
}) 